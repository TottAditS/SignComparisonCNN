# --- Deep Learning PyTorch ---
# GPU build (CUDA 12.8) used in the `bisindo` env -- install via:
#   pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cu128
# CPU-only machines can drop the --index-url and let pip pick the CPU wheel instead.
torch==2.11.0
torchvision==0.26.0

# --- Computer Vision & Image Processing ---
opencv-python==4.11.0.86
numpy==1.26.4
Pillow==12.3.0

# --- Data Analysis & Visualization ---
pandas==3.0.5
matplotlib==3.11.1
seaborn==0.13.2
scipy==1.17.1

# --- Logging & Monitoring ---
tensorboard==2.20.0

# --- Utilities & Evaluation Metrics ---
scikit-learn==1.9.0
tqdm==4.70.0
